/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { SpringBootHelloComponent } from './spring-boot-hello.component';

describe('Component: SpringBootHello', () => {
  it('should create an instance', () => {
    let component = new SpringBootHelloComponent();
    expect(component).toBeTruthy();
  });
});
